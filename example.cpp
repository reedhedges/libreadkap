#include <cstdio>
#include <cstdint>


    // read from stdin:
   // auto default_fread_fn = [](const void* buf, size_t size, size_t nmemb)->int64_t{return (int64_t)fread(buf, size, nmemb, stdin);};

